@model IEnumerable<ClinicaMedica.Consumidor.ViewModels.MedicoViewModel>

@{
    ViewData["Title"] = "Médicos";
}

<h2>Médicos</h2>

@if (TempData["Sucesso"] != null)
{
    <div class="alert alert-success">
        @TempData["Sucesso"]
    </div>
}

@if (TempData["Erro"] != null)
{
    <div class="alert alert-danger">
        @TempData["Erro"]
    </div>
}

@if (ViewData["ErroApi"] != null)
{
    <div class="alert alert-danger">
        <strong>Erro ao consumir a API:</strong><br />
        @ViewData["ErroApi"]
    </div>
}

<p>
    <a asp-action="Create" class="btn btn-primary">Novo Médico</a>
</p>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nome</th>
            <th>Especialidade</th>
            <th>CRM</th>
            <th>Telefone</th>
            <th>Email</th>
            <th>Ativo</th>
            <th>Data Cadastro</th>
            <th style="width: 220px;">Ações</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (var medico in Model)
            {
                <tr>
                    <td>@medico.Id</td>
                    <td>@medico.Nome</td>
                    <td>@medico.Especialidade</td>
                    <td>@medico.Crm</td>
                    <td>@medico.Telefone</td>
                    <td>@medico.Email</td>
                    <td>@(medico.Ativo ? "Sim" : "Não")</td>
                    <td>@medico.DataCadastro.ToString("dd/MM/yyyy HH:mm")</td>
                    <td>
                        <a asp-action="Details" asp-route-id="@medico.Id" class="btn btn-info btn-sm">Detalhes</a>
                        <a asp-action="Edit" asp-route-id="@medico.Id" class="btn btn-warning btn-sm">Editar</a>
                        <a asp-action="Delete" asp-route-id="@medico.Id" class="btn btn-danger btn-sm">Excluir</a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="9" class="text-center">Nenhum médico encontrado.</td>
            </tr>
        }
    </tbody>
</table>